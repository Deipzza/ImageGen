/*

float angle = 0.01;
float r = 200;
int center = 0;

void setup(){
  frameRate(2);
  size(800,600);
  background(255);
}

void draw(){
  /*
  translate(width/2,height/2);
  beginShape();
  vertex(center, r*cos(angle));
  vertex(center, r*sin(angle));
  endShape();
  
  angle += 1;
  
  translate(width/2, height/2);
  beginShape();
  for (float a = 0; a < TWO_PI; a += angle) {
    int contador = 0;
    float rand = random(100);
    if(rand < 1){
      stroke(random(255), random(255), random(255));
      float sx = center + cos(a) * r;
      float sy = center + sin(a) * r;
      vertex(center, center);
      vertex(sx, sy);
    }
    
    if(contador == 100){
      contador = 0;
    }
  }
  endShape();
}

void lineas(){
  
}

*/
